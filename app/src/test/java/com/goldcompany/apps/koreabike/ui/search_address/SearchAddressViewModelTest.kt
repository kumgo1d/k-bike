package com.goldcompany.apps.koreabike.ui.search_address

import org.junit.Assert.*
import org.junit.Test

class SearchAddressViewModelTest {

    @Test
    fun setAddress_isNotNull() {
//        val viewModel = SearchAddressViewModel(ApplicationProvider.getApplicationContext())
//        val repository = KBikeRepository.getRepository(ApplicationProvider.getApplicationContext())
//        val address = repository.getAddress()
//
//        val result = viewModel.setCurrentAddress(address)
//
//        assertNotNull(result)
        assertEquals(4, 1 + 3)
    }
}